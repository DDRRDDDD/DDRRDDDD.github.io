#### 🚨 직면했던 과제
> **"비용 효율성과 정교한 데이터 관리를 동시에 잡아야 한다"**
> * **단순 데이터 구조의 한계**: 단순히 메시지 텍스트만 저장하는 방식으로는, **누가 언제 입장했고 나갔는지**에 대한 정확한 시점을 알 수 없었습니다.
> * **비효율적인 커넥션 관리**: 참여 중인 채팅방 개수만큼 개별 Stream을 연결하는 방식으로 인해, 방 개수가 늘어날수록 **연결 유지 비용과 클라이언트 자원 소모가 증가**할 것을 우려했습니다.

#### 🛠 문제 해결 및 구현 과정
> **💡 이벤트 소싱 모델링과 단일 파이프라인 최적화**
> * **이벤트 기반 데이터 모델링**: 채팅방 내의 모든 상태 변화를 단순 텍스트가 아닌 `대화`, `입장`, `퇴장` 등 **ChatEvent**로 정의하여 통합 관리했습니다. 이를 통해 채팅의 전후 맥락을 추적할 수 있는 구조를 마련했습니다.
> * **메모리 레벨의 필터링**: 앞서 정의한 `입장` 이벤트를 기준점으로 활용하여, 직접 구현한 `takeUntilMemberJoins`를 통해 **입장 시점 이전의 데이터를 클라이언트 메모리 단에서 차단**하는 보안 로직을 완성했습니다.
> * **단일 구독 최적화**: `collectionGroup`와 `array-contains`를 활용해 **단 하나**의 스트림으로 내가 속한 모든 채팅방의 이벤트를 수신하며, 읽기 비용을 줄였습니다.

```dart
// 사용자가 참여 중인 모든 채팅방의 실시간 이벤트를 단일 스트림으로 구독합니다.
Stream<ChatEvent> watchAllChatEvent(String myUid) {
  return _firestore
      .collectionGroup('chat_event')
      .where('member_id', arrayContains: myUid)
      .orderBy('created_at', descending: true)
      .limit(1)
      .snapshots()
      .map((snapshot) => ChatEvent.fromJson(snapshot.docs.first.dataWithDocId()));
}
```
```dart
// 지정한 멤버의 `join` 이벤트가 나오기 전까지의 `ChatEvent`들을 순차적으로 반환하는 확장 함수입니다.
extension ChatEventIterableX on Iterable<ChatEvent> {
  Iterable<ChatEvent> takeUntilMemberJoins(String memberId) sync* {
    for (final ChatEvent chatEvent in this) {
      yield chatEvent;
      if (chatEvent.memberId == memberId &&
          chatEvent.chatEventType == ChatEventType.join) {
        break;
      }
    }
  }
}
```