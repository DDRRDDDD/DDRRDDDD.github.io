### 📂 파일명: 09_아토믹_패턴과_어댑터_구조_개선.md

#### 🚨 직면했던 과제
> **과도한 파라미터 전달 (Props Drilling)** : `ConsumerWidget`을 이용한 컨테이너-프리젠터 패턴을 적용하는 과정에서, 상위 데이터가 하위 컴포넌트까지 도달하기 위해 **데이터가 필요 없는 중간 위젯들까지 파라미터를 계속 중계해야 하는 문제**가 발생했습니다.
> **유지보수의 비효율성** : 데이터 구조가 조금만 바뀌어도, 단순히 값을 전달하기만 하는 중간 위젯들의 코드까지 모두 수정해야 하는 연쇄적인 작업 소요가 발생했습니다.

#### 🛠 문제 해결 및 구현 과정
> **💡 상위 뷰(View) 레벨에서의 스코프 주입**
> * **ProviderScope 오버라이드**: 데이터를 소유한 **상위 뷰 단계에서 `ProviderScope`를 주입**하고 `overrideWithValue`를 사용하여 하위 트리에 데이터를 공급했습니다.
> * **파라미터 전달 제거**: 이를 통해 하위 위젯들이 부모로부터 데이터를 파라미터로 건네받는 과정을 생략하고, 어댑터 내부에서 `ref.watch`로 프로바이더를 직접 참조하여 데이터를 획득하도록 변경했습니다.
> * **어댑터 패턴 결합**: 획득한 데이터는 `ConsumerWidget` 기반의 어댑터가 순수 UI 위젯에 주입하는 형태로 역할을 분리했습니다.

#### 📈 성과/개선
> * **const 위젯 최적화** : 위젯 생성자에서 데이터 파라미터가 제거되면서 `const` 상수를 적용할 수 있게 되었고, 이를 통해 불필요한 리빌드를 방지하여 렌더링 성능을 최적화했습니다.
> * **코드 구조의 단순화** : 중간 단계 위젯들에서 단순히 데이터를 넘겨주기 위해 존재하던 의미 없는 전달 코드를 모두 삭제하여 코드 안정성을 높였습니다.
> * **결합도 감소** : 하위 컴포넌트가 상위 부모 위젯의 데이터 전달 구현 방식에 의존하지 않게 되어, 독립적인 수정과 관리가 가능해졌습니다.

```dart
GoRoute(
    path: 'detail/:id',
    builder: (_, state) => ProviderScope(
    overrides: [
      // 추출한 id 값을 currentIdProvider에 덮어씌움
      // BungaeDetailScreen의 하위 트리에서 bungaeIdProvider로 접근하여 상태 접근
      state.pathParameters
          .lookup('id')
          .let(bungaeIdProvider.overrideWithValue)
    ],
    child: const BungaeDetailScreen(),
  ),
),
```