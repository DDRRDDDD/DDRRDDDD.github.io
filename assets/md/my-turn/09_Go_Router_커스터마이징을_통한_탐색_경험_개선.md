#### 🚨 직면했던 과제
> **사용자의 탐색 맥락은 끊기지 않고 유지되어야 한다"**
> **GoRouter 리다이렉트의 구조적 한계** : GoRouter의 기본 `redirect` 로직은 스택을 교체하는 `go` 방식만 지원하여, 로그인 페이지 진입 시 뒤로 가기가 불가능해지는 등 **이전 화면의 맥락을 보존해야 하는 시나리오에 대응하기 어려운 제약**이 있었습니다.
> **비직관적인 탭 전환 시나리오** : 복합 네비게이션 구조에서 탭 전환 시, 이전의 탐색 맥락을 고려하지 않고 기계적으로 초기화된 뷰를 노출하여 사용자의 흐름이 끊기는 문제가 있었습니다.

#### 🛠 문제 해결 및 구현 과정
> **💡 Push를 지원하는 커스텀 가드 구현**
> * **라우팅 미들웨어 확장**: 프레임워크가 제공하는 기본 리다이렉트 대신, 진입 조건에 따라 `push` 명령을 수행할 수 있는 **커스텀 가드 로직을 미들웨어 형태로 구현**했습니다.
> * **유연한 화면 전환 로직 확보** : `go`와 `push`를 상황에 맞춰 선택적으로 사용할 수 있게 되어, 복잡한 플로우를 제공할 수 있게 되었습니다.
> 
> **💡 중첩된 네비게이션의 상태 보존**
> * **탐색 맥락 유지**: 탭 간 이동 시에도 각 탭의 네비게이션 스택과 스크롤 상태를 그대로 보존하여, 사용자가 **어느 시점에 돌아와도 작업을 이어서 할 수 있는 환경**을 구축했습니다.
> * **탐색 경험 구현** : 복잡한 탭 구조에서도 사용자의 탐색 경로와 데이터가 휘발되지 않고 유지되어, **앱의 사용성이 향상**을 기대할 수 있습니다.

#### 🔗 블로그 링크
* [GoRouter로 똑똑한 리다이렉트(Route Guard) 구현하기](https://velog.io/@ddrrdddd/Flutter-GoRouter%EB%A1%9C-%EB%98%91%EB%98%91%ED%95%9C-%EB%A6%AC%EB%8B%A4%EC%9D%B4%EB%A0%89%ED%8A%B8Route-Guard-%EA%B5%AC%ED%98%84%ED%95%98%EA%B8%B0)