#### ❝데이터 전달을 위해 위젯 구조가 강제되는 현상을 해결하고, 컴포넌트의 독립성을 확보했습니다.❞

#### 🚨 문제(Problem)
> * 상위 위젯에서 하위 위젯으로 데이터를 계속 전달하는 Drilling으로 인해 결합도가 증가하고 const 최적화가 불가능해짐.

#### 🛠 해결(Solution)
> * **Scoped Provider 적용** : `ProviderScope(overrides: [...])`를 사용해 특정 서브트리에 의존성을 주입.
> * **생성자 다이어트** : 위젯 생성자에서 데이터 파라미터를 제거하고 내부에서 `ref.watch`로 직접 접근하도록 리팩토링.

코드 필요

#### 📈 성과(Outcome)
> * 위젯 트리의 `const` 사용률 증가로 프레임워크 수준 캐싱 최적화 달성.
> * 컴포넌트 결합도 감소로 유지보수성 및 재사용성 향상.
