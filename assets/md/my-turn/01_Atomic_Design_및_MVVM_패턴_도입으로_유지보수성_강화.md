#### 🚨 직면했던 과제
> **❝비즈니스 로직의 클라이언트 종속과 명세의 부재가 협업과 확장의 병목이 되었습니다.❞**
> * **클라이언트에 비대해진 비즈니스 로직**: 서버 로직이 클라이언트 코드에 강하게 결합되어 있어, 데이터 구조 변경 시 앱 전체를 수정해야 했습니다. 또한, 기획자가 정의한 비즈니스 규칙을 개발자가 코드로 옮기는 과정에서 **오차와 누락이 빈번히 발생**했습니다.
> * **변경에 취약한 백엔드 의존성**: 위와 더불어 특정 BaaS(Firebase) SDK에 직접 의존하는 구조로 인해, 향후 **서버 인프라 변경이나 마이그레이션이 불가능**한 상태였습니다.
> * **추상적인 디자인 소통**: "조금 더 진하게", "큰 제목" 등 모호한 표현으로 소통하다 보니, 디자이너의 의도가 코드에 정확히 반영되지 않아 **UI 검수와 수정에 과도한 시간이 소요**되었습니다.

#### ⚙️ 아키텍처
![워크플로우 드리븐 아키텍처](assets/md/my-turn/workflow_driven_design_diagram.png "워크플로우 아키텍처")

#### 🛠 문제 해결 및 구현 과정
> **💡협업을 위한 디자인 시스템**
> * **Design Token 기반의 아토믹 디자인**: 디자이너와 **Design Token(Color, Typography)**을 공통 언어로 정의하여 모호함을 제거했습니다. 이를 바탕으로 아토믹 디자인 패턴(Atom, Molecule...)을 적용해 컴포넌트를 시스템화함으로써, 디자인 수정 사항이 코드에 즉각적이고 정확하게 반영되도록 했습니다.
>
> **💡비즈니스 로직 분리와 아키텍처 확장성**
> * **RPC를 활용한 로직 캡슐화**: Supabase RPC를 도입하여 핵심 비즈니스 로직을 서버로 이관했습니다. 비즈니스 모델에 대한 이해도가 높은 **기획자가 직접 SQL/RPC 함수를 작성**하게 하여 로직의 정확도를 높였고, 클라이언트는 결과값만 받아 처리하도록 경량화했습니다.
> * **Repository 패턴과 인터페이스 추상화**: `Supabase` 구현체에 직접 의존하지 않고, **Interface(Repository)를 통해 데이터 계층을 추상화**했습니다. 이를 통해 향후 백엔드가 변경되더라도 비즈니스 로직 수정 없이 구현체만 교체하면 되는 유연한 아키텍처를 완성했습니다.
>
> **💡상태 관리의 최적화 (Architecture)**
> * **읽기/쓰기 계층의 분리**: `NotifierProvider`로 상태 변경을 담당하는 ViewModel과, `select`를 활용해 데이터 조회 및 렌더링 최적화를 담당하는 **Read-only Layer**를 분리하여 불필요한 리빌드를 구조적으로 차단했습니다.

#### 📈 성과/개선
> * **기획-개발 병렬 프로세스 구축** : 서버 로직(RPC)을 기획자가 주도적으로 관리하게 되면서, 개발자는 UI와 클라이언트 아키텍처에만 집중할 수 있어 **전체 프로젝트 진행 속도가 획기적으로 향상**되었습니다.
> * **유지보수 비용 절감** : 디자인 토큰과 아토믹 패턴 덕분에 디자인 변경 요청 시 단일 토큰 수정만으로 앱 전체 스타일을 제어할 수 있게 되었습니다.
> * **벤더 종속성 탈피** : 인터페이스 기반 설계로 특정 BaaS에 종속되지 않는 확장 가능한 백엔드 구조를 확보했습니다.