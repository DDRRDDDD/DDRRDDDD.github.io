#### ❝사용자의 무의식적인 반복 행동이 서버에 주는 부담을 클라이언트 단에서 제어했습니다.❞

#### 🚨 문제(Problem)
> * 사용자 입력(검색어 타이핑, 버튼 연타)으로 인해 짧은 시간에 다수의 이벤트가 발생하고, 이는 불필요한 API 호출과 레이스 컨디션을 유발함.

#### 🛠 해결(Solution)
> * **요청 시점 제어** : 검색 입력에는 Trailing Debounce(300ms)를 적용해 입력 완료 시점에만 요청 실행.
> * **버튼 액션** : 버튼 액션에는 **Leading Debounce**를 적용해 첫 클릭만 수용하도록 처리.

#### 📈 성과(Outcome)
> * 불필요한 API 호출 횟수 약 70% 감소.
> * 중복 데이터 생성 및 컨디션(Race Condition) 문제 완화.
