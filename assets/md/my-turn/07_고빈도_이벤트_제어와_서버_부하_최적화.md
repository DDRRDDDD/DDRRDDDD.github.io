#### 🚨 직면했던 과제
> **"무분별한 사용자 입력으로 인한 리소스 낭비와 데이터 오염"**
> * **네트워크 대역폭 낭비**: 검색어 자동완성과 같이 입력 갱신이 잦은 기능에서, 의미 없는 중간 단계의 타이핑까지 모두 API를 호출하여 **불필요한 트래픽과 서버 리소스 소모**가 발생했습니다.
> * **중복 요청 발생**: 버튼을 사용자가 습관적으로 연타할 경우, **중복 요청으로 인한 데이터 정합성 깨짐**이나 비즈니스 로직 오류가 발생할 위험이 있었습니다.

#### 🛠 문제 해결 및 구현 과정
> **💡 상황별 이벤트 제어 전략**
> * **Debounce (검색 최적화)**: 검색어 입력과 같이 '최종 결과값'이 중요한 로직에는, 입력이 완전히 멈춘 뒤 마지막 요청만 전송하는 **디바운스**를 적용해 불필요한 중간 API 호출을 차단했습니다.
> * **Throttle (무한 스크롤 최적화)**: 스크롤이 임계점에 도달하면 **즉시 첫 번째 요청을 실행**하되, 이후 설정된 시간(쿨다운) 동안 발생하는 추가 이벤트는 무시하는 **스로틀링**을 적용했습니다. 이를 통해 로딩 반응성은 유지하면서 중복 페이지 호출은 최소화했습니다.

#### 📈 성과/개선
> * **API 호출 효율화** : 검색어 입력 중 발생하는 무의미한 중간 호출을 모두 필터링하고 유효한 요청만 전송하여, **네트워크 트래픽 낭비를 최소화**했습니다.
> * **데이터 무결성 확보** : 클라이언트 단에서 물리적인 중복 이벤트를 필터링함으로써, 서버 부하를 줄임과 동시에 **중복 데이터 생성 및 동시성 문제를 효과적으로 예방**했습니다.

```dart
// 디바운스 로직
class Debouncer implements EventGuard {
  Timer? _timer;

  @override
  void call(VoidCallback callback, {Duration? duration}) {
    if (_timer?.isActive ?? false) {
      _timer?.cancel();
    }
    
    _timer = Timer(duration ?? _defaultDuration, callback);
  }

  @override
  void close() {
    _timer?.cancel();
  }
}
```
```dart
// 쓰로틀링 로직
class Throttler implements EventGuard {
  Timer? _timer;
  bool _isWaiting = false;

  @override
  void call(VoidCallback callback, {Duration? duration}) {
    if (_timer?.isActive ?? false) {
      return;
    }

    callback();
    _timer = Timer(duration ?? _defaultDuration, () {});
  }

  @override
  void close() {
    _timer?.cancel();
  }
}
```