### 📂 파일명: 11_리스트_뷰_전환을_통한_스크롤_성능_개선.md

#### 🚨 직면했던 과제
> **레이아웃 연산 집중으로 인한 프레임 저하**
> * **비효율적인 전체 계산**: `SingleChildScrollView`와 `Column` 조합은 화면에 보이지 않는 영역의 아이템까지 미리 레이아웃(크기 및 위치) 계산을 수행합니다.
> * **Wrap 위젯의 부하**: 특히 아이템마다 포함된 `Wrap` 위젯은 줄바꿈 처리를 위해 연산 비용이 높은데, 이를 진입 시점에 한꺼번에 처리하려다 보니 화면이 일시적으로 멈추는 현상(Jank)이 발생했습니다.

#### 🛠 문제 해결 및 구현 과정
> **💡 ListView 도입으로 연산 시점 분산**
> * **렌더링 방식 변경**: 전체 높이를 미리 확정해야 하는 `Column` 대신, 화면에 보이는 영역만 채우는 방식인 `ListView`로 구조를 변경했습니다.
> * **지연된 레이아웃(Lazy Layout)**: `ListView` 내부적으로 화면 밖 요소들의 레이아웃 연산을 수행하지 않는 특성을 활용하여, `Wrap` 위젯의 무거운 연산이 스크롤 시점에 나누어 처리되도록 만들었습니다.

#### 📈 성과/개선
> * **진입 지연 해결** : 불필요한 초기 연산을 제거하여, 데이터가 많은 목록 화면에 진입할 때 발생하던 로딩 렉을 해결했습니다.
> * **스크롤 안정성 확보** : 레이아웃 연산 부하가 적절히 분산됨에 따라, 복잡한 UI 구성에서도 끊김 없이 부드러운 스크롤 경험을 제공했습니다.